data/
__pycache__/
*.pyc
.pytest_cache/
.env
# latest/ NO ignorado — se commitea para GitHub Pages
# mounts.db SI se commitea para que esté disponible via Pages
# Pero los archivos auxiliares de SQLite no:
mounts.db-wal
mounts.db-shm
mounts.db-journal
# Cache HTTP local (rebuildeable, no necesita ir al repo)
.cache/
# Audit dump (research, no scope productivo)
_audit/
# Estado local de Claude Code (worktrees, settings locales) — no va al repo
.claude/
